server:
  port: 8080

spring:
  application:
    name: spring-boot-demo-cache-ehcache

  # 缓存配置
  cache:
    type: ehcache
    ehcache:
      config: classpath:ehcache.xml

# 日志配置
logging:
  level:
    com.yqsh.springboot.demo.cache: debug
